/**
 * Home Screen Types
 * 
 * TypeScript interfaces and types for the Home screen component.
 */

/**
 * User statistics interface for the dashboard
 */
export interface UserStats {
  sessionCount: number;
  isVerified: boolean;
}

/**
 * Home screen props interface
 */
export interface HomeScreenProps {
  // Currently no props needed, but available for future use
} 